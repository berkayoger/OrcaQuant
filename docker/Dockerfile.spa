FROM node:20-alpine AS build
WORKDIR /app
COPY frontend_spa/package.json frontend_spa/package-lock.json ./
RUN npm ci
COPY frontend_spa ./
RUN npm run build

FROM alpine:3.20
WORKDIR /dist
COPY --from=build /app/dist ./
CMD ["sh", "-c", "echo 'Built SPA artifact ready in /dist'; sleep 2;"]
